---
- name: Harden all nodes
  hosts: all
  become: true
  roles:
    - base-hardening

- name: Install k3s control plane
  hosts: control_plane
  become: true
  roles:
    - k3s-server

- name: Join worker nodes
  hosts: workers
  become: true
  roles:
    - k3s-agent
